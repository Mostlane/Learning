# Setup Notes (Stage 1)

1. Confirm frontend scaffold files exist in `frontend/pages`, `frontend/css`, and `frontend/js`.
2. Confirm Worker scaffold exists in `worker/src/index.js` and `worker/wrangler.toml`.
3. Confirm API base URL in `frontend/js/config.js` points to:
   - `https://job-application.jamie-def.workers.dev`
4. Open a page (e.g. `frontend/pages/login.html`) and verify shared theme/card/button/timeline render.
